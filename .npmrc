# Build native modules (better-sqlite3) against Electron's headers, not system Node.
# electron-builder's install-app-deps (postinstall) handles the actual rebuild.
node-linker=hoisted
public-hoist-pattern[]=*
