# Native modules (better-sqlite3) are rebuilt against Electron's ABI by the
# postinstall hook (`electron-builder install-app-deps`), so a plain `npm install`
# is all that's needed. No extra config required here.
